[package]
name = "rule"
version = "0.1.0"
edition = "2024"

[dependencies]
typetag = "0.2.21"
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1.0", features = ["sync", "rt"] }
async-trait = "0.1"

[dev-dependencies]
tokio = { version = "1.0", features = ["full"] }
