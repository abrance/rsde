apiVersion: v1
kind: Service
metadata:
  name: {{ include "file-transfer-go.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "file-transfer-go.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "file-transfer-go.selectorLabels" . | nindent 4 }}
